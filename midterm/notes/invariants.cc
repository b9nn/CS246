#include <iostream>
#include <string>
#include <sstream>
#include <fstream>
using namespace std;

// invariants are porperties of a data structure that must be true
// encapsulation is what enforce these invariants, so the user can interact with the code correctly

// private fields can be utilized within a class and only a class

// within a class everything is private unless specified otherwise
// within a struct everything is public unless specified otherwise


