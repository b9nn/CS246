/* principle of design patterns: program to interfaces, not impls!
 - abstract base class defines the iterfacae
 - work with base class pointers or references and define methods
    - concrete subclass are swappable with methods any method that uses base class pointers/refs (similar to virtual)


 */